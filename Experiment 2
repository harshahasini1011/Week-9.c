#include <stdio.h>
int gcd(int a, int b) {
if(b == 0)
return a;
return gcd(b, a % b);
}
int main() {
int a, b;
printf("REG NO:25331A05F7\n");
printf("enter the numbers:");
scanf("%d %d", &a, &b);
printf("gcd=%d", gcd(a, b));
}
